#pragma once

const int SCREEN_WIDTH = 800;
const int SCREEN_HEIGHT = 600;
const int GROUND_Y = 536; // Position where the ground starts
const float GRAVITY = 0.25f;
const float JUMP_FORCE = -10.0f; // Slightly stronger jump for the ground
const float PLAYER_SPEED = 5.0f;